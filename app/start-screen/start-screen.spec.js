'use strict';

describe('treeGameApp.startScreen module', function() {

  beforeEach(module('treeGameApp.startScreen'));

  describe('start-screen controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('StartScreenCtrl');
      expect(ctrl).toBeDefined();
    }));

  });
});
